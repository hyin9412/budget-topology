# 方案二：弧线、高亮、透明度降低逻辑总结

本文基于当前 `index.html` 的真实实现整理，供前端复用或重写时参考。

## 1. 方案二当前走的是哪条代码路径

- `state.scheme === "scheme2"` 时，不走 `方案一` 的共享总线逻辑，也不走 `方案三` 的目标侧合计标签逻辑。
- 当前 `方案二` 走的是通用的 `legacy layout` 分支：
  - `buildGraph()`
  - `buildRegularGraphLayout()`
  - `buildRegularGraphLayoutLegacy()`
- 这条分支的核心特点是：
  - 先生成一条“正交折线骨架”
  - 再把这条骨架通过三次贝塞尔曲线平滑成“弧线效果”
- 所以从视觉上看是“弧线连接”，但实现上并不是随手画一条自由曲线，而是“基于折线路径的平滑曲线”。

## 2. 方案二的弧线连线逻辑

### 2.1 端口和骨架点的生成

在 `buildRegularGraphLayoutLegacy()` 里，每条边都会先计算以下关键点：

- `sourcePortX / sourcePortY`
  - 源节点的出线点
- `targetPortX / targetPortY`
  - 目标节点的入线点
- `sourceExitY`
  - 从源节点先垂直拉出一小段独占区，避免刚出节点就挤在一起
- `targetEntryY`
  - 进入目标节点前也先保留一小段独占区
- `laneY`
  - 中间水平通道的 Y 坐标

最终骨架点固定是 6 个：

1. 源节点端口
2. 源节点独占垂直段终点
3. 源侧接入中间通道点
4. 目标侧离开中间通道点
5. 目标节点独占垂直段起点
6. 目标节点端口

也就是一个标准的：

```text
节点 -> 垂直段 -> 中间水平通道 -> 垂直段 -> 节点
```

### 2.2 laneY 的分配方式

如果多条边处在同一个“上下通道区间”，不会完全压在同一条水平线上。

当前做法是：

- 先按 `sourceExitY / targetEntryY` 生成 `corridorKey`
- 把处在同一 corridor 的边归到一个组里
- 同组边再按 `sourcePortX`、`targetPortX` 排序
- 通过 `LANE_GAP` 给每条边分配一个略有偏移的 `laneY`

这样可以避免多条边完全重叠。

### 2.3 弧线是怎么画出来的

弧线效果来自 `smoothCurvePath(points)`。

当前实现不是逐段圆角，而是：

- 保留骨架的起点和终点方向
- 根据首尾方向向量计算两个控制点 `cp1 / cp2`
- 最后直接输出一段三次贝塞尔曲线：

```text
M start
C cp1, cp2, end
```

这意味着：

- 视觉是平滑弧线
- 连线仍然继承“正交骨架”的整体走向
- 起点和终点方向会更像从节点边缘“自然拉出来”

## 3. 方案二的高亮逻辑

方案二的高亮不是单独实现的，而是复用全局交互系统。

### 3.1 hover 连线时

触发入口：

- `renderEdge()` 给每条边的 `edge-group` 绑定 `mouseenter / mouseleave`
- `mouseenter` 时调用 `setActiveEdge(edge.id)`

`setActiveEdge()` 会把当前激活态设置成：

- `edgeIds = 当前这条边`
- `nodeIds = 当前边的 source + target`

也就是：

- 高亮这条边
- 同时高亮它两端的节点

### 3.2 hover 节点时

触发入口：

- `renderNode()` 给每个节点绑定 `mouseenter / mouseleave`
- `mouseenter` 时调用 `setActiveNode(node.id)`

`setActiveNode()` 会扫描当前图里所有边：

- 只要某条边的 `source === nodeId` 或 `target === nodeId`
- 就把这条边加入 `edgeIds`
- 并把这条边两端节点都加入 `nodeIds`

也就是：

- hover 一个节点时，会高亮所有与该节点直接相连的边
- 同时这些边两端涉及到的节点也会进入激活态

## 4. 方案二的高亮表现

### 4.1 连线颜色加深

样式上通过：

- `.edge-group.is-active .edge-route`

实现以下变化：

- `stroke` 从默认 `--line` 切到 `--line-hover`
- `marker-end` 从默认箭头切到 hover 箭头

当前不是蓝色高亮，而是“灰线轻微加深”。

### 4.2 激活边提升到前景层

在 `applyActiveState()` 里：

- 激活边会被挪到 `activeEdgeLayer`
- 非激活边留在 `edgeLayer`

这么做的作用是：

- 激活边永远压在普通边上面
- 即使多条边交叉，hover 的那条也不会被盖住

### 4.3 节点高亮

节点同样通过 `is-active` 和前景层切换处理：

- 激活节点会挪到 `activeNodeLayer`
- 非激活节点仍在 `nodeLayer`

这样高亮节点也能浮在其它节点之上。

## 5. 方案二的透明度降低逻辑

透明度降低同样复用全局机制。

### 5.1 边和节点的整体降透明度

`applyActiveState()` 在有激活对象时，会把：

- `backdropGroup.style.opacity = 0.3`

这里的 `backdropGroup` 包含：

- 普通边层 `edge-base`
- 普通节点层 `node-base`

这意味着：

- 所有“未被提升到前景层”的边和节点都会整体降到 30%
- 激活边和激活节点因为被移动到前景层，所以不会一起变淡

### 5.2 金额 Tag 的透明度单独控制

因为金额 `Tag` 现在位于前景层，不属于 `backdropGroup`，所以不能只靠父层降透明度。

当前额外做了单独处理：

- 清除激活态时：
  - 所有 `tag-group` 恢复 `opacity = 1`
- 有激活态时：
  - 与激活边关联的 `tag-group` 保持 `opacity = 1`
  - 非关联 `tag-group` 直接设置 `opacity = 0.3`

这里的关联判断依据是：

- `data-edge-id`
- 或 `data-edge-ids`

对于方案二来说，通常是一条 Tag 对应一条边，所以判断比较直接。

## 6. 方案二金额 Tag 与高亮的关系

方案二的金额 Tag 不是合计模式，仍然是一边一个 Tag。

逻辑上：

- Tag 会绑定在这条边自己的 `data-edge-id` 上
- hover 节点时，如果该边被选中，对应 Tag 保持 100%
- 其它不在当前高亮范围内的 Tag 会降到 30%

所以最终表现是：

- 高亮边 + 对应节点 + 对应金额 Tag
- 其它边、节点、Tag 一起降透明度

## 7. 如果前端要复刻，建议保留的状态模型

建议保留下面这套最小状态：

```ts
type ActiveState = {
  edgeIds: Set<string>;
  nodeIds: Set<string>;
} | null;
```

并按下面规则处理：

### hover 边

- 激活当前边
- 激活当前边的 source / target 节点

### hover 节点

- 激活所有与该节点直连的边
- 激活这些边涉及到的所有节点

### 透明度

- 无激活态：全部 `opacity = 1`
- 有激活态：
  - 激活边 / 节点 / Tag：`opacity = 1`
  - 非激活边 / 节点 / Tag：`opacity = 0.3`

### 层级

- 激活边放到更高层
- 激活节点放到更高层
- Tag 保持在边之上

## 8. 前端实现建议

如果要重写而不是照搬现有代码，建议：

- 连线路径继续保留“骨架点 + 平滑曲线”的思路
  - 这样布局规则和视觉效果更容易同时控制
- 高亮和透明度不要分别写多套逻辑
  - 统一从一个 `activeState` 推导
- 图层至少分三层：
  - 普通边/节点层
  - 激活边层
  - Tag 层或激活节点层
- Tag 透明度单独控制
  - 不要假设它一定跟边在同一个父层

## 9. 相关代码位置

- 连线骨架与 lane 分配：`index.html` 中的 `buildRegularGraphLayoutLegacy()`
- 弧线生成：`index.html` 中的 `smoothCurvePath()`
- hover 激活入口：`setActiveEdge()`、`setActiveNode()`
- 高亮/透明度主逻辑：`applyActiveState()`
- 连线 hover 样式：`.edge-group.is-active .edge-route`

